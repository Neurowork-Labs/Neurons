# Completed tasks

## Project API keys

- **Project API Keys tab** (`/project/[projectId]/api-keys`): Server-generated keys, `public.project_api_keys` (`key_hash` + `key_prefix`), list/search/pagination, create with optional expiry, single active key per project with confirmation to deactivate the previous active key, one-time plaintext reveal (no integration snippet on that screen; see pending work for Project Overview snippet).
